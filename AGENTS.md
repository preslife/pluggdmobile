# Codex Instructions

- Use judgment to decide when side branches, separate threads, or sub-agents are useful.
- Default to a side branch for implementation work.
- Use sub-agents only when a task has clearly independent parts that can be investigated or implemented in parallel.
- Never merge into `main` without explicit user approval.
- Keep `main` untouched unless the user explicitly asks for a merge.
