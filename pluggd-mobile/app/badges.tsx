import { SimpleContextScreen } from '../src/features/culture/CultureScreens';

export default function BadgesScreen() {
  return (
    <SimpleContextScreen
      title="Badges / Rewards"
      subtitle="Fan identity, rewards and cultural proof."
      icon="workspace-premium"
      body="Badges, rewards, event attendance and fan identity signals will appear here when supported by the backend."
    />
  );
}
